
# Exercício 1 : 

segundos = int(input("Digite o número de segundos: "))

horas = segundos // 3600

minutos = (segundos % 3600) // 60

seg_restantes = segundos % 60

print(horas, "hora,", minutos, "minuto e", seg_restantes, "segundos.")


# Exercício 2 :

num1 = int(input("Digite o primeiro número: "))
num2 = int(input("Digite o segundo número: "))
num3 = int(input("Digite o terceiro número: "))

maior = num1
menor = num2

if num2 > maior:
    maior = num2

if num3 > maior:
    maior = num3

# Verificar o menor
if num2 < menor:
    menor = num2

if num3 < menor:
    menor = num3

    print("Maior:", maior)
print("Menor:", menor)